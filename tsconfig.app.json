{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": ["jasmine", "node"]  // Añadir las definiciones de Jasmine
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.ts",   // Incluir todos los archivos .ts en la carpeta src
    "src/**/*.d.ts"  // Incluir archivos de declaración también
  ]
}
